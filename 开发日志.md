# 开发日志

## 第一阶段:基础架构 + 数据层 + 核心 UI + 数据逻辑 ✅

**完成功能**
- 项目脚手架:`pyproject.toml`(依赖 PySide6)、目录结构(`app/{ui,database,services,utils,tests}`)
- 数据库层:`models.py`(表结构)、`database.py`(连接管理 + 初始化 + 默认种子数据)
  - 表:`categories` / `transactions` / `budgets` / `savings_goals` / `settings`
  - 交易表对分类外键 `ON DELETE SET NULL`,删除分类不破坏账单
- 服务层:
  - `finance_service` 记账/账单/余额
  - `budget_service` 预算/周期/建议/提醒(80%/100%/超支)
  - `statistics_service` 分类统计/每日趋势/月度对比
  - `prediction_service` 月底预测(加权统计法,可替换算法)
  - `savings_service` 储蓄目标
  - `category_service` 分类管理
  - `settings_service` 导入导出 JSON/CSV + 备份恢复
- 工具层 `helpers.py`:金额格式化、预算周期计算、全局样式表
- UI 层:主窗口(侧边栏导航)+ Dashboard / 记账 / 统计 / 预算 / 储蓄 / 设置 六页

## 第二阶段:MVP 全功能 ✅

**完成功能**
- 首页 Dashboard:本月剩余预算(大字)、预算进度条、距离周期结束天数、今日建议消费、月底预测、累计余额、超支提醒、储蓄目标概览
- 快速记账:金额/分类/日期/备注 + 支出/收入切换,一键添加,自动刷新
- 账单列表:按类型/分类筛选、备注搜索、双击编辑、删除确认
- 预算管理:月度预算、自然月/自定义周期、实时使用情况与提醒
- 基础统计:分类占比饼图、每日消费趋势柱状图、月度对比表

## 第三阶段:高级功能 ✅

**完成功能**
- 月底消费预测:融合周期日均(0.4)+ 近 7 日日均(0.6),预计总消费/余额/超支金额
- 储蓄目标:新增/编辑/删除、存入/取出、进度条
- 月度对比:本月 vs 上月各分类,带涨跌箭头
- 数据导入导出:JSON 全量(合并/替换模式)、CSV 账单、SQLite 备份、从备份恢复(均有确认)

## 发现并修复的 Bug

1. **`category_service.add_category` SQL 多一个 `?`** —— `VALUES(?,?,?,?,0,?)` 6 值插 5 列 → 改为 `VALUES(?,?,?,0,?)`
2. **交易金额未拒绝 0** —— 服务层只拒负数 → 改为拒绝 `<= 0`(金额必须大于 0)
3. **`to_date(None)` 崩溃** —— None/空日期直接 `fromisoformat` 报错 → 默认返回今天
4. **`QPieSlice.LabelInsideHorizontal` 枚举不存在** —— 新版 PySide6 命名空间变更 → 用 `LabelPosition.InsideHorizontal` 并 try 兼容
5. **表格 `cellDoubleClicked` 在 `refresh` 内重复连接** —— 每次刷新叠加信号 → 移到 `_build` 只连一次(transaction_page、settings_page 均修复)
6. **`main_window._build_sidebar` 冗余死代码** —— `nav_container` hack 与未用布局 → 清理
7. **测试 Windows 文件锁(PermissionError)** —— 失败用例未关连接导致后续删不掉 DB → `_setup` 先显式关闭连接、runner 用 `try/finally` 保证清理

## 测试

- `app/tests/test_services.py`:27 项单元测试,覆盖金额格式化、周期计算、记账增删改、负数/0/巨额/跨月/空账单、预算阈值、月底预测、统计、储蓄、分类删除带账单、导入导出、备份恢复 —— **全部通过**
- `app/tests/test_ui_smoke.py`:offscreen 平台构造主窗口 + 切换刷新所有页面 + 记账/储蓄/预算全流程 —— **全部通过**
- 真实平台启动:运行 6 秒未崩溃,GUI 正常渲染

## 修改的文件

```
pyproject.toml
README.md
启动.bat
app/main.py
app/utils/helpers.py
app/database/models.py
app/database/database.py
app/services/finance_service.py
app/services/budget_service.py
app/services/statistics_service.py
app/services/prediction_service.py
app/services/savings_service.py
app/services/category_service.py
app/services/settings_service.py
app/ui/widgets.py
app/ui/dashboard.py
app/ui/transaction_page.py
app/ui/statistics_page.py
app/ui/budget_page.py
app/ui/savings_page.py
app/ui/settings_page.py
app/ui/main_window.py
app/tests/test_services.py
app/tests/test_ui_smoke.py
开发日志.md
```

## 下一阶段计划(MVP 稳定后的迭代候选)

按需求"不要为增加功能而增加功能",以下仅在确有用时再实现:
- 智能记账:自然语言解析("午饭 18" → 自动识别金额/分类/日期)
- 固定支出提醒、超支推送
- 月度财务报告自动生成
- 多账户管理
- 更稳健的预测算法(替换 `prediction_service.Predictor` 即可,接口已抽象)

---

# 迭代二:产品打磨与自主迭代(2026-08-21)

以「真实软件产品」标准全面审查并迭代,优先级 靓定性 > 易用性 > 数据安全 > 实用功能。

## 修复的 Bug / 数据完整性
- **快速记账数据不同步**:记账后只刷新账单页,Dashboard/预算不更新 → 改为 `refresh_all` 全页同步
- **操作无反馈**:记账/删除/编辑/储蓄操作后无提示 → 主窗口加状态栏 toast,各操作反馈"已记账 ¥28""已删除"等
- 异常提醒误报:过去四周基线数据稀疏(仅1天)时,正常消费被误判为异常 → 要求基线至少 3 天有消费才算

## 新增功能
1. **新手引导卡**:零账单时 Dashboard 显示欢迎 + 三步引导按钮(设预算/记账/储蓄目标)
2. **账单空状态**:无账单或筛选无结果时显示友好提示
3. **智能每日建议**(`budget_service.smart_daily_suggestion`):融合持平线(0.5)+ 近7日日均(0.5),永不超过持平线(不鼓励超支);Dashboard 展示"近期偏快/偏省"节奏
4. **月度财务报告**(`statistics_service.monthly_report`):本月收入/支出/结余、最大消费类别、环比上月、超支、储蓄进度;统计页「📄 本月财务报告」对话框
5. **固定周期收支**(`recurring_service` + `recurring` 表):生活费/话费/会员等,到账日首页提醒,一键记入,每月只提醒一次(不自动创建,避免数据意外);设置页「固定收支」标签管理
6. **消费异常提醒**(`anomaly_service`):今日消费明显高于近7日日均、本周某类高于过去四周周均;首页静默卡片,不弹窗

## 可维护性
- 验证 UI/逻辑分离:所有 service 均不 import UI 或 PySide6
- `prediction_service` 消除 `predict`/`predict_with_budget` 重复逻辑,抽出 `_compute_prediction` + `_recent_stats`,顺带修复潜在除零
- 修复多测试模块同会话运行的 DB 路径覆盖问题(各 `_setup` 显式重指向本模块临时库)

## 测试
- `test_services.py`:50 项(新增智能建议、月度报告、固定收支、异常提醒等)
- `test_lifecycle.py`:端到端生命周期(新用户→记账→改/删→跨月→储蓄→改预算→导出→**关闭重开验证持久化**→月度报告→异常提醒)
- `test_ui_smoke.py`:offscreen 构造+全页刷新+记账流程
- 全部通过:51 单元 + 1 生命周期 + 烟雾 ALL OK + 真实启动 6s 不崩溃

## 新增/修改文件
```
app/services/recurring_service.py      (新)
app/services/anomaly_service.py        (新)
app/services/budget_service.py         (智能建议 + recent_daily)
app/services/statistics_service.py     (monthly_report)
app/services/prediction_service.py     (去重重构)
app/database/models.py                 (recurring 表)
app/ui/dashboard.py                    (引导/提醒/异常卡 + 智能建议展示)
app/ui/transaction_page.py             (空状态 + 反馈 + 同步)
app/ui/savings_page.py                 (操作反馈)
app/ui/statistics_page.py             (报告对话框 + 按钮)
app/ui/settings_page.py               (固定收支标签)
app/ui/main_window.py                 (状态栏 feedback)
app/tests/test_services.py            (扩充到 50 项)
app/tests/test_lifecycle.py            (新)
```

---

# 迭代三:智能自然语言记账 + 极致减少记账步骤(2026-08-21)

核心目标:让「记一笔账」从 7 步(点新增→输入→选类型→选分类→选日期→填备注→保存)
压缩为「输入一句话 → 回车 → 确认」,3 秒内完成。

## 新增功能
1. **自然语言解析器**(`app/services/natural_language_parser.py`):本地规则引擎,无 AI API
   - `parse("午饭18")` → {amount:18, type:expense, category:餐饮, date:今天, note:午饭}
   - 支持金额(整数/小数,取最后一个数字)、日期(今天/昨天/前天/YYYY年MM月DD日/8月20日)、收入识别(生活费/兼职/奖学金/红包/到账)
   - 关键词→分类映射可扩展(`add_keyword_to_map`),不在 UI 硬编码
   - 置信度:高/中/低(有金额+日期确定+分类明确=高;上周=中;无金额=低)
   - 永不崩溃:任何输入都返回结构化结果(abc→金额None,今天吃饭→提示补金额)
2. **首页快速记账条**:输入框自动聚焦 + Enter 提交 + 「记账」按钮 + Ctrl+K 全局快捷键
3. **确认对话框**(`SmartInputDialog`):展示解析结果,所有字段可编辑,一键确认,确认后自动清空输入并重新聚焦
4. **全链路数据同步**:确认记账 → 写库 → refresh_all(余额/今日消费/预算/统计/预测/异常提醒全部更新)+ 状态栏反馈

## 关键设计
- 解析器与 UI 完全分离,`parse()` 可独立测试
- 日期不确定(如"上周买东西120")不留默认日期,让用户在确认框核对,数据准确性优先
- 无法识别分类 → "其他";无金额 → 置信度低并提示补充,不阻止记账
- 无年份的"8月20日"默认今年

## 测试
- `test_natural_language_parser.py`:30 项(金额/日期/分类/收入/自然语言变体/异常输入/置信度)
- `test_ui_smoke.py` 新增自然语言全链路(PASS)
- 回归全部通过:30 解析器 + 51 单元/生命周期 + 烟雾 ALL OK + 真实启动 6s

## 新增/修改文件
```
app/services/natural_language_parser.py  (新)
app/ui/smart_input.py                    (新:确认对话框)
app/ui/dashboard.py                      (首页快速记账条 + focus_input + Ctrl+K)
app/ui/main_window.py                    (Ctrl+K 快捷键)
app/tests/test_natural_language_parser.py (新)
app/tests/test_ui_smoke.py               (自然语言全链路)
```

## 完成标准验证
打开软件 → 首页输入「午饭18」→ 回车 → 弹出确认框(支出¥18/餐饮/今天/备注午饭)→ 确认 →
当前余额、今日消费、预算、统计、月底预测全部即时更新 ✓

---

# 迭代四:长期使用体验、记账效率与产品细节优化(2026-08-21)

目标:连续使用数周/数月后依然方便、稳定,并逐渐贴合个人习惯。

## 新增与优化功能
1. **自然语言金额边界修复**:`奶茶.16→0.16`、`奶茶.5→0.5`、`午饭0018→18`;负数(`-20`)金额无效;0 金额不可记;空格变体正常
2. **最近使用分类 + 智能建议**:分类下拉按近 30 天使用频率排序;无法明确分类时(`消费50`)推荐高频分类,但**不擅自写入**,低置信度强制确认
3. **常用记账模板**:默认 🍜午饭/🧋奶茶/🚇地铁/🛒购物,点击只输金额即记账;设置页可增/删/改/上下移排序;首页最多显示 6 个 + 「管理」
4. **首页最近记录**:最近 5 条展示,双击进编辑框(可改/可删),不必专门进账单页
5. **确认框快捷键**:任意字段 Enter=确认记账、Esc=取消;解析正确聚焦金额(再按一次 Enter 完成);不确定时自动聚焦待改字段;底部提示按键
6. **周期自动切换 + 月初/月末体验**:自然月/自定义周期/跨年自动切换,历史保留、新旧周期隔离;月初显示"新周期已开始"+收入口径;月末(≤3 天)显示"周期将结束,当前剩余/预计结余"
7. **个人使用习惯统计**:最易记账时间段、日均笔数、最大单笔(含分类)、消费集中度 —— 放月度报告,不进 Dashboard
8. **数据安全**:自动备份(每 7 天,保留 5 个,空库不重复备份,不影响启动);数据库损坏时启动自动从最近有效备份恢复;恢复/备份前清理 WAL 侧文件防止旧状态串扰
9. **首页精简**:删除重复的"快速记账"头部按钮与"累计余额/超支提醒"两张卡,累计余额与超支状态并入 hero 副标题 → 首页层级:剩余金额 → 今日建议/周期结束 → 快速记账 → 最近记录/提醒

## 发现并修复的问题
- **`auto_backup(force=True)` 节流误伤**:7 天节流未用 `not force` 守卫,连点"立即备份"被挡住 → 修复
- **数据库损坏恢复失效**:`get_connection()` 在损坏文件上抛 `DatabaseError`(WAL 前置检查)未被 init_db 捕获;`_connect` 未关闭坏连接会锁文件 → init_db 整体包 try、坏连接先 close 再抛
- **恢复后数据为 0(WAL 串扰)**:恢复/覆盖前未清 `-wal/-shm` 侧文件,旧 WAL 作用到新文件 → 恢复/备份前清理侧文件
- **跨测试模块 DB 路径快照串扰**:`settings_service` import 时快照 `DB_PATH`,多测试模块先导入者决定路径 → 改为运行时动态读取 `_db_path()`
- **测试失败级联**:单个失败测试的 `_teardown` 未执行导致状态泄漏污染后续测试 → 加 autouse 清理 fixture

## 关键架构调整
- `settings_service` 的 DB 路径改为**运行时读取**(不再 import 快照)—— 使恢复/备份/自动备份始终作用于当前库
- 自动备份/损坏恢复成为独立、可测试的能力(保留最近 5 个,7 天一轮)

## 测试与结果
- 新增:金额边界(空格/前导点/前导零/负数/0)、最近分类按使用排序、模板增删改排、自动备份轮换与节流、损坏恢复、跨月/跨年周期切换、习惯统计
- **95 项全部通过**(解析器 36 + 单元 58·含生命周期与备份恢复 + 新增周期/模板/建议/恢复)
- 烟雾测试 ALL OK(全页构造 + 自然语言 + 模板 + 最近记录)
- 真实启动 6s 不崩溃 ✓

## 性能测试(100 / 500 / 1000 / 5000 条)
| 操作 | 5000 条耗时 |
|---|---|
| init_db(启动) | 5.7ms |
| Dashboard 汇总 | 1.5ms |
| 账单查询(前 500) | 1.6ms |
| 分类统计 | 2.5ms |
| 每日趋势 | 4.4ms |
| 月度报告 | 2.1ms |
| 自然语言解析 | 0.1ms |

全部 ≤10ms,索引良好,无需优化;长期累积数千条仍流畅。

## 暂时未解决的问题
- 无已知阻塞问题。Bash 分类器在环境侧间歇不可用,曾短暂影响"最终真实启动"复跑,但已早前验证成功且 95 测试+烟雾覆盖了所改全部路径。
- 「最近使用分类优先」已在确认框推荐中体现;如需在快速记账条本身记住上次分类可后续再加(受益边际小,未做)。


---

# 迭代五:系统级全面优化与产品化(2026-08-21)

目标:从 Demo 级升级为结构清晰、启动快速、UI 统一、长期可维护的桌面软件。
流程:先审计→基线→问题分级(P0/P1/P2/P3)→分阶段优化→每阶段验证。

## 审计结论(优先问题分级)
- **架构**:Service 层零依赖 UI/PySide6 ✅;DB 操作集中在 database.py ✅;核心计算可独立测试 ✅
- **P1**:①MainWindow 启动即建 6 页+一次性导入全部页面模块(统计/设置/账单重模块拖累首屏);②refresh_all 每次记账全量重算统计页图表
- **P2**:①settings_page(703行)/dashboard(573行)偏大但已按职责拆 _refresh_*;②分类聚合 SQL 三处重复;③无日志系统;④各对话框宽度魔法数字
- **P3**:3 个死函数 + 批未用导入

## 实际修改
1. **启动懒加载**:页面惰性创建(首屏仅建首页,其余首次导航创建)+ 模块惰性导入;`ui.main_window` 导入 71.6→20.5ms,`MainWindow()` 只含 Dashboard;首页已加载页面=['dashboard']
2. **刷新精细**:`refresh_all` 只刷当前可见页+首页,其余页导航时自动刷新(《确保数据一致》由 _on_nav_changed 保证)
3. **日志系统**:`utils/logging_setup.py` 滚动文件日志(1MB×3,~/.college_finance/logs),记录启动/DB恢复/备份/导入导出错误/未捕获异常;默认 INFO 无打扰
4. **死码清理**:删除 `database.get_db_path`、`helpers.style_card`、`settings_service.backup_database` 及 pyflakes 全部未用导入
5. **重复 SQL 抽取**:`statistics_service.category_expense_rows` 统一,分类占比/月度对比/异常提醒三处复用
6. **Design Tokens**:`utils/design_tokens.py`(留白/字号/圆角/按钮高/对话框宽),6 页面根布局 + 对话框宽度统一到令牌;同时统一了页面间距差异(16→14)

## 性能(优化前 → 优化后,offscreen 基线)
- 启动(不含解释器):约 411ms → 约 189ms(含字节码缓存变暖;结构性收益=重模块移出首屏,PySide6 ~110-195ms 为框架固定成本)
- Dashboard 首次刷新:3.2ms → 1.0ms
- 5000/10000 条:全部核心操作 ≤10ms(查询 1-4ms),无需更深度优化

## 验证
- 95 项测试全部通过(单元+生命周期+解析器)、烟雾 ALL OK、真实启动 6s 正常
